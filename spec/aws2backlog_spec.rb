require 'spec_helper'

describe Aws2backlog do
  it 'has a version number' do
    expect(Aws2backlog::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
